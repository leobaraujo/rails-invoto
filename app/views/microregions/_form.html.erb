<%= form_with(model: microregion) do |form| %>
  <% if microregion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(microregion.errors.count, "error") %> prohibited this microregion from being saved:</h2>

      <ul>
        <% microregion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :region_id, style: "display: block" %>
    <%= form.collection_select :region_id, Region.all, :id, :name, prompt: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
